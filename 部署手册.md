# NAI Gallery — The Glasshouse 部署手册

本文档用于在 Cloudflare Pages 上首次部署该网站。所有指令均在项目根目录 `C:\work\nai-gallery` 下运行。
真实账户动作（登录、建库、注入 secret）由站长本人执行，代码与本文档公开、真实凭据绝不入库。

## 前置条件

- 已安装 Node.js 18+ 与 npm
- 已 `npm install`
- 拥有 Cloudflare 账号、Discord OAuth 应用（拿到 client id/secret）

## 一、首次部署顺序

### 1. 登录 Cloudflare
```powershell
npx wrangler login
```

### 2. 创建 D1 数据库并回填 id
```powershell
npx wrangler d1 create nai-gallery
```
把返回的 `database_id` 复制粘贴进 [wrangler.toml](wrangler.toml) 的 `database_id` 字段。

### 3. 建表（远程）
```powershell
npx wrangler d1 execute nai-gallery --remote --file=migrations/0001_initial.sql
```
本地预建一遍（可选，验证 SQL）：
```powershell
npx wrangler d1 execute nai-gallery --local --file=migrations/0001_initial.sql
```

### 4. 创建 R2 桶
```powershell
npx wrangler r2 bucket create nai-gallery-bucket
```

### 5. 配置 R2 CORS（浏览器直传 PUT 必须）
```powershell
npx wrangler r2 bucket cors put nai-gallery-bucket --file r2-cors.json
```
`r2-cors.json` 已随仓库提供，请把其中的 origins 改成你的真实 dev/生产域名。

### 6. 注入 Secrets
用 `npx wrangler pages secret put <NAME>` 逐个注入（首次会提示选项目）：
```powershell
npx wrangler pages secret put DISCORD_CLIENT_ID --project-name nai-private-gallery
npx wrangler pages secret put DISCORD_CLIENT_SECRET --project-name nai-private-gallery
npx wrangler pages secret put SESSION_SECRET --project-name nai-private-gallery
npx wrangler pages secret put PUBLIC_BASE_URL --project-name nai-private-gallery
npx wrangler pages secret put R2_ACCOUNT_ID --project-name nai-private-gallery
npx wrangler pages secret put R2_BUCKET_NAME --project-name nai-private-gallery
npx wrangler pages secret put R2_ACCESS_KEY_ID --project-name nai-private-gallery
npx wrangler pages secret put R2_SECRET_ACCESS_KEY --project-name nai-private-gallery
npx wrangler pages secret put IP_HASH_SECRET --project-name nai-private-gallery
```
值参考 [.env.example](.env.example) 的注释。`COOKIE_NAME` 可用默认值，不必注入。
`DISCORD_REDIRECT_URI` 与 `DISCORD_REDIRECT_URI` 需在 Discord Developer Portal 里设为生产域名。

### 7. 构建并部署
```powershell
npm run build
npx wrangler pages deploy dist --project-name nai-private-gallery
```
首次 `pages deploy` 若项目不存在会让你输入项目名并自动创建。完成后会给出生产域名。

## 二、管理员初始化

1. 用 Discord OAuth 在生产域名登录一次（首页点登录），这会在 `users` 表生成你的行。
2. 拿到你的 Discord ID 后，远程提权：
```powershell
npx wrangler d1 execute nai-gallery --remote --command="UPDATE users SET role='admin' WHERE discord_id='你的DiscordID';"
```
3. 刷新页面即可看到 Admin 入口（导航栏底部）。

## 三、本地验证（本地全链路）

`npm run cf:dev` 会启动 wrangler pages dev，用本地 Miniflare D1（`.wrangler/state`）+ Miniflare R2，跑通真实 Functions。
```powershell
npm install
npx wrangler d1 execute nai-gallery --local --file=migrations/0001_initial.sql
npm run build
npm run cf:dev
```
打开 http://localhost:8788 验证：
- `/api/me` 应返回 JSON `{ user: null }`（非 HTML，证明 `public/_routes.json` 已放行 API）。
- Dev Mode 按钮（首页）可 mock 登录为 admin。
- 进入 `/admin` 走完「创建批次 -> 上传 PNG -> 分组预览 -> 提交」全链路，签名 URL 指向 Miniflare R2。

## 四、日常更新与重新部署

```powershell
npm run build
npx wrangler pages deploy dist --project-name nai-private-gallery
```
改库表：新增 `migrations/NNNN_*.sql`，对远程 `npx wrangler d1 execute nai-gallery --remote --file=...`。
改 secret：重跑第 6 步对应命令即可覆盖。

## 五、故障排查

- **API 返回 HTML/SPA：** 检查 [public/_routes.json](public/_routes.json) 的 exclude 里是否误含 `/api/*`（已在本次移除）。
- **管理员直传 R2 失败、CORS 错误：** 确认 `npx wrangler r2 bucket cors put` 已执行，且 r2-cors.json 的 origins 含你的前端域名。
- **登录后角色仍是 user：** D1 里 `SELECT discord_id, role FROM users;` 确认提权生效。
- **预签名 URL 过期：** `DOWNLOAD_URL_TTL_SECONDS` 默认 120 秒，缓存下载链接会失效是正常的。

## 六、安全提示

- R2 桶保持私有（不开放公开读），图片只通过预签名 URL 下发。
- `SESSION_SECRET` 与 `IP_HASH_SECRET` 用独立随机串（`openssl rand -hex 32`），不要复用。
- 批次密码创建后仅明文显示一次，数据库只存 PBKDF2 哈希。
- 每次解锁/下载/收藏都写审计日志（access_logs、downloads_log），可查人、时间、图。
