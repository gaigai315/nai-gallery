# NAI Gallery 部署宝宝教程

这是一份给第一次部署网站的人看的教程。你不需要先懂 Cloudflare、数据库或服务器，按顺序复制命令即可。

本文默认你使用 Windows，并且项目文件夹是：

```text
C:\work\nai-gallery
```

如果你的项目放在别的地方，后面所有命令都要在那个项目文件夹里执行。

## 先认识几个东西

- **GitHub**：放代码的地方。它不是网站服务器。
- **Cloudflare Pages**：把网站发布到互联网上。
- **D1**：Cloudflare 提供的小型数据库，保存用户、批次和日志。
- **R2**：Cloudflare 提供的文件存储，保存图片。它必须保持私有。
- **Discord OAuth**：让用户用 Discord 登录。
- **Secret**：密码和密钥。只输入到 Cloudflare，不要写进代码，也不要发给别人。

整套部署完成后，大致关系是：

```text
用户浏览器 -> Cloudflare Pages -> 网站和 API
                              -> D1 数据库
                              -> 私有 R2 图片仓库
                              -> Discord 登录
```

## 0. 开始前的安全规则

请先记住四句话：

1. 任何真实密码、Discord Client Secret、R2 Secret Access Key 都不能放进 GitHub。
2. `.env.example` 可以公开，`.dev.vars` 不可以公开。
3. R2 桶不要设置成公开访问。
4. 如果某个密钥不小心发到聊天、截图或 GitHub，立刻去对应平台撤销并重新生成。

## 1. 安装必需软件

### 1.1 安装 Node.js

打开 Node.js 官网：<https://nodejs.org/>，下载 **LTS** 版本并安装。安装时一路点默认选项即可。

安装完成后，打开 Windows 的 **PowerShell**：

1. 点击开始菜单。
2. 搜索 `PowerShell`。
3. 打开 Windows PowerShell。

输入下面两行：

```powershell
node -v
npm -v
```

如果都显示版本号，就安装成功了。如果提示“不是内部或外部命令”，请关闭 PowerShell，重新打开一次；仍然不行就重新安装 Node.js。

### 1.2 进入项目文件夹

在 PowerShell 中输入：

```powershell
cd C:\work\nai-gallery
```

看到命令行路径变成 `C:\work\nai-gallery>`，就说明位置正确。

### 1.3 安装项目依赖

```powershell
npm install
```

第一次可能需要几分钟。看到命令执行结束、没有红色报错即可。

## 2. 先在本地试玩网站

这一步不会发布到互联网，也不会使用真实数据库，建议先做。

```powershell
npm run dev
```

浏览器打开：<http://localhost:5173>

首页如果有 **Dev Mode**，可以点击它模拟管理员登录。你可以先熟悉页面、导航和管理后台。

停止本地网站时，回到 PowerShell 按 `Ctrl + C`。

## 3. 创建 Cloudflare 账号并登录

打开 <https://dash.cloudflare.com/> 注册或登录 Cloudflare。

建议先确认 Cloudflare 账号已经添加付款方式。D1 和 R2 有免费额度，但超出额度后可能产生费用；正式上传大量图片前，请在 Cloudflare 控制台查看当前套餐和用量。

回到项目 PowerShell，执行：

```powershell
npx wrangler login
```

浏览器会弹出 Cloudflare 授权页面。点击允许，看到授权成功后回到 PowerShell。

如果它问是否允许打开浏览器，选择允许即可。

## 4. 创建 Cloudflare Pages 项目

先用第一次部署创建项目。执行：

```powershell
npm run build
npx wrangler pages deploy dist --project-name nai-private-gallery
```

如果提示项目不存在，选择创建项目；项目名填写：

```text
nai-private-gallery
```

部署结束后会显示一个 `https://...pages.dev` 地址。先复制保存这个地址，后面会用到。这个地址就是网站的临时公网地址。

## 5. 创建 D1 数据库

执行：

```powershell
npx wrangler d1 create nai-gallery
```

输出内容里会有类似下面的字段：

```text
database_id = "一长串字母和数字"
```

打开项目里的 [wrangler.toml](wrangler.toml)，找到：

```toml
database_id = "replace-with-your-d1-database-id"
```

把引号里的内容替换成刚才那一长串 ID，然后保存文件。

### 5.1 初始化数据库表

项目现在有多份数据库迁移文件，必须按顺序全部执行。每执行一条，看到没有报错再执行下一条：

```powershell
npx wrangler d1 execute nai-gallery --remote --file=migrations/0001_initial.sql
npx wrangler d1 execute nai-gallery --remote --file=migrations/0002_notes_cover.sql
npx wrangler d1 execute nai-gallery --remote --file=migrations/0003_announcements_config.sql
npx wrangler d1 execute nai-gallery --remote --file=migrations/0004_vibe_prompts.sql
npx wrangler d1 execute nai-gallery --remote --file=migrations/0005_wishes_feedback.sql
npx wrangler d1 execute nai-gallery --remote --file=migrations/0006_filter_words.sql
```

这里的 `--remote` 意思是操作云端数据库，不是本地测试数据库。

## 6. 创建 R2 图片仓库

执行：

```powershell
npx wrangler r2 bucket create nai-gallery-bucket
```

如果提示名称已存在，换一个全球唯一的名字，例如：

```text
你的名字-nai-gallery-images
```

如果你换了名字，必须同时修改 [wrangler.toml](wrangler.toml) 里的：

```toml
bucket_name = "你的新桶名称"
```

以及后面 Secret 里的 `R2_BUCKET_NAME`。

### 6.1 配置 R2 CORS

打开 [r2-cors.json](r2-cors.json)，把：

```text
https://your-production-domain.pages.dev
```

替换成你的真实 Pages 地址。例如：

```text
https://nai-private-gallery.pages.dev
```

保留 `http://localhost:5173` 和 `http://localhost:8788`，以后本地调试还会用到。

保存后执行：

```powershell
npx wrangler r2 bucket cors put nai-gallery-bucket --file=r2-cors.json
```

如果你用了其他桶名称，把命令里的 `nai-gallery-bucket` 一起替换掉。

## 7. 创建 Discord 登录应用

打开 <https://discord.com/developers/applications>，登录 Discord。

1. 点击 **New Application**。
2. 名称可以填写 `NAI Gallery`。
3. 进入左侧 **OAuth2**。
4. 复制 **Client ID**，后面叫 `DISCORD_CLIENT_ID`。
5. 点击 **Add Redirect**，填写：

```text
https://你的-pages-地址.pages.dev/api/auth/callback
```

例如：

```text
https://nai-private-gallery.pages.dev/api/auth/callback
```

6. 点击保存。
7. 在 **Client Secret** 旁边点击生成或重置，然后复制它。它只属于你自己，后面叫 `DISCORD_CLIENT_SECRET`。

不要把 Client Secret 发给任何人，也不要提交到 GitHub。

### 可选：限制只有你的 Discord 服务器成员能登录

如果网站只给自己的 Discord 服务器使用，可以额外配置服务器 ID和角色 ID。第一次部署建议先跳过，等网站跑通后再配置。

如果要配置：

1. Discord 设置 -> 高级 -> 打开 **开发者模式**。
2. 右键你的服务器 -> **复制服务器 ID**，这就是 `DISCORD_GUILD_ID`。
3. 右键允许登录的角色 -> **复制角色 ID**，这就是 `DISCORD_REQUIRED_ROLE_ID`。
4. 在 Discord OAuth2 的 Scopes 中增加 `guilds` 和 `guilds.members.read`。

## 8. 准备 Cloudflare Secrets

下面这些值需要输入到 Cloudflare。执行每条命令后，PowerShell 会让你输入对应值；输入时屏幕可能不显示字符，这是正常的，输入完成后按回车即可。

### 8.1 必填项

先把下面的 `https://你的-pages-地址.pages.dev` 换成你的真实地址。

```powershell
npx wrangler pages secret put DISCORD_CLIENT_ID --project-name nai-private-gallery
npx wrangler pages secret put DISCORD_CLIENT_SECRET --project-name nai-private-gallery
npx wrangler pages secret put DISCORD_REDIRECT_URI --project-name nai-private-gallery
npx wrangler pages secret put SESSION_SECRET --project-name nai-private-gallery
npx wrangler pages secret put PUBLIC_BASE_URL --project-name nai-private-gallery
npx wrangler pages secret put R2_ACCOUNT_ID --project-name nai-private-gallery
npx wrangler pages secret put R2_BUCKET_NAME --project-name nai-private-gallery
npx wrangler pages secret put R2_ACCESS_KEY_ID --project-name nai-private-gallery
npx wrangler pages secret put R2_SECRET_ACCESS_KEY --project-name nai-private-gallery
npx wrangler pages secret put IP_HASH_SECRET --project-name nai-private-gallery
```

每个字段应该填写：

| 字段 | 填什么 |
| --- | --- |
| `DISCORD_CLIENT_ID` | Discord 应用的 Client ID |
| `DISCORD_CLIENT_SECRET` | Discord 应用的 Client Secret |
| `DISCORD_REDIRECT_URI` | `https://你的-pages-地址.pages.dev/api/auth/callback` |
| `SESSION_SECRET` | 随机长密码，至少 32 个字符 |
| `PUBLIC_BASE_URL` | 网站首页地址，例如 `https://nai-private-gallery.pages.dev` |
| `R2_ACCOUNT_ID` | Cloudflare 控制台右侧或 R2 页面显示的 Account ID |
| `R2_BUCKET_NAME` | R2 桶名称，默认是 `nai-gallery-bucket` |
| `R2_ACCESS_KEY_ID` | R2 API Token 的 Access Key ID |
| `R2_SECRET_ACCESS_KEY` | R2 API Token 的 Secret Access Key |
| `IP_HASH_SECRET` | 另一条随机长密码，不能和 `SESSION_SECRET` 相同 |

生成随机密码可以在 PowerShell 执行：

```powershell
[guid]::NewGuid().ToString('N') + [guid]::NewGuid().ToString('N')
```

执行两次，第一次结果给 `SESSION_SECRET`，第二次结果给 `IP_HASH_SECRET`。

### 8.2 创建 R2 API Token

在 Cloudflare 控制台：

1. 进入 **R2**。
2. 找到 **Manage R2 API Tokens**。
3. 创建一个只允许访问这个图片桶的 token。
4. 复制 Access Key ID 和 Secret Access Key。

Secret Access Key 通常只显示一次。复制后放到上面对应的命令中，不要截图或发给别人。

### 8.3 可选项

如果你想限制 Discord 服务器成员，执行：

```powershell
npx wrangler pages secret put DISCORD_GUILD_ID --project-name nai-private-gallery
npx wrangler pages secret put DISCORD_REQUIRED_ROLE_ID --project-name nai-private-gallery
```

`COOKIE_NAME`、`DOWNLOAD_URL_TTL_SECONDS`、`DOWNLOAD_LIMIT_PER_HOUR` 可以先使用代码默认值，不需要现在配置。

## 9. 重新部署网站

配置完数据库、R2 和 Secrets 后，执行：

```powershell
npm run build
npx wrangler pages deploy dist --project-name nai-private-gallery
```

打开你的 Pages 地址。如果首页能打开，说明网站已经发布。

## 10. 第一次登录并设置管理员

1. 打开网站首页。
2. 点击 Discord 登录。
3. 授权 Discord。
4. 登录成功后，回到 PowerShell 执行：

```powershell
npx wrangler d1 execute nai-gallery --remote --command="SELECT discord_id, username, role FROM users;"
```

复制你自己的 `discord_id`。

然后执行下面的命令，把 `你的DiscordID` 替换成真实 ID：

```powershell
npx wrangler d1 execute nai-gallery --remote --command="UPDATE users SET role='admin' WHERE discord_id='你的DiscordID';"
```

刷新网页，应该能看到 Admin 管理入口。

## 11. 创建第一个图片批次

1. 进入 Admin。
2. 创建一个批次。
3. 页面会显示一次批次密码，立即复制并保存到密码管理器。
4. 进入上传区域，选择成对的 PNG 和 TXT 文件。
5. 等上传完成后，用批次密码测试普通访问流程。

批次密码只显示一次。忘记密码时，可以在后台重置，但旧密码会失效。

## 12. 本地完整测试（可选）

如果你只想先上线，可以跳过这一节。

执行：

```powershell
npm run build
npx wrangler d1 execute nai-gallery --local --file=migrations/0001_initial.sql
npx wrangler pages dev dist --d1=DB --r2=GALLERY_BUCKET
```

浏览器打开 <http://localhost:8788>。

本地测试和线上数据库相互独立。你在本地创建的数据不会出现在正式网站上。

## 13. 以后更新网站

每次修改代码后，在项目目录执行：

```powershell
npm run build
npx wrangler pages deploy dist --project-name nai-private-gallery
```

如果修改了数据库结构，新增一份 `migrations/0007_说明.sql` 这样的迁移文件，并只对远程数据库执行一次：

```powershell
npx wrangler d1 execute nai-gallery --remote --file=migrations/0007_说明.sql
```

不要重复执行已经执行过的迁移文件。

## 14. 常见问题

### `npm` 或 `node` 找不到

重新安装 Node.js LTS，安装后关闭并重新打开 PowerShell。

### Discord 登录后跳回首页并显示失败

检查三处是否完全一致：

1. Discord OAuth2 的 Redirect URL。
2. `DISCORD_REDIRECT_URI` 的值。
3. 当前访问的网站域名。

三处必须都是同一个地址，并且结尾都包含 `/api/auth/callback`。

### 网站能打开，但图片上传失败

检查：

1. R2 桶是否存在。
2. `wrangler.toml` 的桶名和 `R2_BUCKET_NAME` 是否一致。
3. 是否执行过 R2 CORS 命令。
4. `r2-cors.json` 是否包含当前网站地址。
5. R2 是否保持私有，但 API Token 是否有这个桶的读写权限。

### 登录成功，但没有 Admin 入口

重新执行查询，确认自己的 Discord ID 和角色：

```powershell
npx wrangler d1 execute nai-gallery --remote --command="SELECT discord_id, username, role FROM users;"
```

如果角色还是 `user`，重新执行第 10 节的提权命令，然后刷新网页或重新登录。

### API 返回 HTML，而不是 JSON

先重新构建并部署：

```powershell
npm run build
npx wrangler pages deploy dist --project-name nai-private-gallery
```

如果仍然有问题，检查 [public/_routes.json](public/_routes.json) 是否保留了 `/api/*` 的 API 路由配置。

### 预览或下载链接过一会儿失效

这是正常的。图片通过短时间有效的签名 URL 保护，不是永久公开链接。重新打开图片即可生成新的链接。

## 15. 最后检查清单

- [ ] 网站首页可以打开。
- [ ] Discord 可以登录。
- [ ] 你自己的账号已经改成 `admin`。
- [ ] 可以创建批次。
- [ ] 批次密码已私下保存。
- [ ] 可以上传并预览图片。
- [ ] 普通用户不能直接访问 R2 桶。
- [ ] `.env`、`.dev.vars`、真实密钥没有提交到 GitHub。
- [ ] R2 CORS 中没有遗留不属于你的生产域名。

## 安全提醒

- R2 桶保持私有，图片通过签名 URL 临时访问。
- `SESSION_SECRET` 与 `IP_HASH_SECRET` 要使用两条不同的随机字符串。
- 不要把真实 Secret 写进 `wrangler.toml`、README、截图或 GitHub Issue。
- 如果怀疑密钥泄露，立即在 Discord 或 Cloudflare 中重新生成并重新执行对应的 `wrangler pages secret put` 命令。
- 项目中的图片、提示词和用户上传内容不一定属于代码许可证范围，发布前请确认你拥有相应使用权。
