# NAI Gallery — The Glasshouse 总要求

## 一、项目概述

搭建一个私密的 NAI 画图分享画廊网站。给固定小圈子的人共享 AI 生成的图片，每批图有独立密码，谁在什么时间下载/收藏了什么图都有日志可查。

品牌名为 **The Glasshouse**，视觉风格是毛玻璃 + 柔和色调，支持日夜间切换。

---

## 二、核心原则

1. **代码公开、数据私有** — 前端代码完全开源，但真实的图片数据和访问密码绝不外泄（R2 私有桶 + 预签名 URL）
2. **双重验证** — Discord 登录（身份凭证）+ 批次密码（内容门禁），两者缺一不可
3. **全程溯源** — 每次解锁、下载、收藏都写入数据库，精确到人、时间、图片
4. **NAI 原生支持** — 上传时自动解析 PNG metadata（prompt、seed、参数），自动按正负面 prompt 分组管理，但这些分组在浏览端不展示

---

## 三、用户流程

### 3.1 普通用户
```
首页（Discord 登录）
  -> 书架页（Tarot 叠卡 / 瀑布流浏览各批次）
    -> 点击中央卡片 -> 密码弹窗
      -> 输入密码验证 -> 进入该批次画册内部
        -> 网格 / 翻页书双模式浏览图片
          -> 点击图片 -> 毛玻璃详情弹窗
            -> 分别复制正负面 prompt、下载原图/TXT、收藏
```

### 3.2 管理员（额外）
```
书架页底部/导航栏显示 Admin 入口
  -> 管理后台
    -> 创建新批次、生成密码
    -> 批量上传 PNG/TXT
    -> 自动解析 metadata + 按 prompt 分组
    -> 手动调整分组后提交
    -> 启用/停用批次
```

---

## 四、功能清单

### 已完成（后端 + 数据库 + 前端组件结构）

**后端 API（全部就绪）**
- Discord OAuth 登录/登出
- Session 管理（HMAC 签名 Cookie）
- 批次密码验证（PBKDF2）
- 画廊图片列表
- 下载限频 + 审计日志
- 收藏/取消收藏
- 管理员创建批次、管理批次
- 管理员上传签名、提交 manifest
- R2 预签名 URL 生成（GET / PUT）

**数据库（全部就绪）**
- users（含 role 列）
- batches
- prompt_groups（正负面 prompt 分组）
- images（含 seed、metadata、R2 key）
- user_batch_unlocks
- favorites、access_logs、downloads_log

**前端组件（结构完成，对接 mock 数据中）**
- 玻璃导航栏（响应式、hover 展开）
- 塔罗牌叠卡/瀑布流书架
- 密码验证弹窗
- 图片网格/翻页书双模式
- 毛玻璃详情弹窗（正负面分别复制+流光动画）
- 日夜间切换
- Dev 模式本地预览

### 待完成

**基础设施**
- Cloudflare Pages SPA fallback 配置（_routes.json）

**API 数据对接**
- 书架页对接真实批次列表
- 画册页对接真实图片数据
- 详情窗对接下载/收藏接口

**NAI 元数据解析**
- 浏览器端 PNG tEXt chunks 解析
- 提取 prompt、seed、参数
- 解析失败降级为文件名导入

**Admin 上传面板**
- 批次管理（创建/查看/启用/停用）
- 批量上传 PNG/TXT
- 自动 metadata 解析 + prompt 分组预览
- 手动分组编辑（移动/合并/新建/重命名）
- 浏览器端 WebP 缩略图生成
- 直传 R2 + 提交 manifest

**密码弹窗 UI**
- 集成用户提供的密码锁设计稿

---

## 五、数据组织方式

```
画廊（The Glasshouse）
  └─ 批次（Batch）— 如 "2026-10 月度合集"
       └─ Prompt 组（prompt_groups）— 后端+上传用，前端不展示
            └─ 图片（Image）
```

- 批次按什么组织（时间/主题/混合）待确认
- 前端画廊扁平展示图片网格
- 自动分组仅在后端和管理上传流程使用

---

## 六、设计语言

- 品牌色：柔和大地色系（米白 #F7F1E5、鼠尾草绿 #C6D0B4、橄榄绿 #7A8B64）
- 暗色模式：深黑底 #0A0A0A + 暖棕灰字
- 玻璃质感：backdrop-filter blur + 半透明 border
- 圆角为主（16px-24px），避免直角
- 动画平滑（cubic-bezier 0.4/0/0.2/1）
- 响应式：PC 左上悬浮导航、移动端底部通栏导航
