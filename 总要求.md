# NAI Gallery — The Glasshouse 总要求

## 一、项目概述

搭建一个私密的 NAI 画图分享画廊网站。给固定小圈子的人共享 AI 生成的图片，每批图有独立密码，谁在什么时间下载/收藏了什么图都有日志可查。

品牌名为 **The Glasshouse**，视觉风格是毛玻璃 + 柔和色调，支持日夜间切换。

---

## 二、核心原则

1. **代码公开、数据私有** — 前端代码完全开源，但真实的图片数据和访问密码绝不外泄（R2 私有桶 + 预签名 URL）
2. **双重验证** — Discord 登录（身份凭证）+ 批次密码（内容门禁），两者缺一不可
3. **全程溯源** — 每次解锁、下载、收藏都写入数据库，精确到人、时间、图片
4. **NAI 原生支持** — 上传时自动解析 PNG metadata（prompt、seed、参数），自动按正负面 prompt 分组管理，分组名和备注在浏览端内页展示

---

## 三、用户流程

### 3.1 普通用户
```
首页（密令宣誓 → Discord 登录）
  -> 公告弹窗 → 书架页（Tarot 叠卡 / 瀑布流浏览各批次）
    -> 点击中央卡片 -> 密码弹窗
      -> 输入密码验证 -> 进入该批次画册内部
        -> 网格 / 翻页书双模式浏览图片
          -> 点击图片 -> 毛玻璃详情弹窗
            -> 分别复制正负面 prompt、下载原图/TXT、收藏
```

### 3.2 管理员（额外）
```
书架页底部/导航栏显示 Admin 入口
  -> 管理后台
    -> 创建新批次、生成密码
    -> 批量上传 PNG/TXT
    -> 自动解析 metadata + 按 prompt 分组
    -> 手动调整分组后提交
    -> 启用/停用批次
```

---

## 四、功能清单

### ✅ 已完成

**后端 API（全部就绪）**
- Discord OAuth 登录/登出
- Session 管理（HMAC 签名 Cookie）
- 批次密码验证（PBKDF2）
- 画廊图片列表
- 下载限频 + 审计日志
- 收藏/取消收藏
- 管理员创建批次、管理批次
- 管理员上传签名、提交 manifest
- R2 预签名 URL 生成（GET / PUT）

**数据库（全部就绪）**
- users（含 role 列）
- batches
- prompt_groups（正负面 prompt 分组）
- images（含 seed、metadata、R2 key）
- user_batch_unlocks
- favorites、access_logs、downloads_log

**基础架构**
- [x] Cloudflare Pages SPA fallback 配置（_routes.json，API 走 Functions）

**画廊核心**
- [x] 书架页对接真实批次列表（含 image_count/group_count）
- [x] 画册页对接真实图片数据
- [x] 详情窗对接下载/收藏接口
- [x] 塔罗牌叠卡/瀑布流书架
- [x] 密码验证弹窗（PasswordSeal 组件）
- [x] 图片网格/翻页书双模式（浏览端内页展示分组列表+备注）

**NAI 元数据解析**
- [x] src/lib/png-metadata.js — 浏览器端 PNG tEXt chunks 解析
- [x] 提取 prompt、seed、参数（别名映射 uc→negative_prompt 等）
- [x] 解析失败降级为文件名导入

**Admin 上传面板**
- [x] 批次管理（创建/查看/启用/停用/删除）
- [x] 批量上传 PNG/TXT
- [x] 自动 metadata 解析 + prompt 分组预览
- [x] 手动分组编辑（移动/合并/新建/重命名）
- [x] 浏览器端 WebP 缩略图生成
- [x] 直传 R2 + 提交 manifest（src/lib/upload.js）
- [x] 统计仪表盘（总用户/总解锁/总下载）
- [x] 管理员免密进入批次 + 主页删除弹窗增强
- [x] 封面自定义 + 批次备注 + 分组备注

**架构优化**
- [x] 密码验证性能优化 — verify.js 改为按 batch_id 精确查询
- [x] 后端限流 — verify/download/favorite 加频率限制
- [x] 内存限流器过期清理 — rate-limit.js 自动清理过期 key
- [x] 前端错误处理 — 全局 ApiError toast 提示
- [x] 图片级别管理 — AdminView 图片浏览/删除/移动/压缩归档 + 上传格式选择(PNG/JPEG)
- [x] 画廊分页 — gallery API 支持 offset/limit

**社区功能**
- [x] 公告系统 — 管理员编辑多条公告（排队、带图），用户登录后弹窗轮播
- [x] 密令宣誓 — 管理员自定义宣誓文本，新用户须逐字打出才能登录
- [x] 搜索 — 全局搜索 prompt / 串名 / Vibe / 提示词，跳转对应内容
- [x] 视图偏好记忆 — 记住滚动位置、Tarot/Grid 选择，默认 Grid
- [x] 收藏查看 — /favorites 路由 + FavoritesView

**内容模块**
- [x] Vibe 专区 — 后端+前端（VibeListView/VibePostView），管理员可发帖/编辑/删除，上传文件
- [x] 提示词专区 — 后端+前端（PromptListView/PromptPostView），一键复制 prompt
- [x] 模块导航 — ModuleNav 组件，顶部胶囊：画廊 | Vibe | 提示词
- [x] 搜索覆盖层 — SearchOverlay 全屏毛玻璃
- [x] Vibe/提示词联调 — 上传管线接通（POST → compressToJpeg → upload-sign → PUT → PATCH）
- [x] 抽卡 — DrawView（魔法阵翻牌，随机抽取已解锁画廊图片）
- [x] 许愿墙 — WishView（便利贴网格，站长回复，最多3张图）
- [x] 反馈 — FeedbackView（纯表单收集，仅管理员可见）
- [x] 关于作者 — AboutView（仿个人主页，管理员可编辑头像/背景/简介/Discord链接/作者的话）

**UX 收尾**
- [x] 删除按钮 hover 才显示（TarotDeck + WaterfallGrid）
- [x] 批次备注移到密码弹窗（PasswordSeal）
- [x] 导航栏重构（三区一行）+ 搜索覆盖层（SearchOverlay）
- [x] AdminView 设置图标换齿轮
- [x] 桌面 Grid 尺寸优化
- [x] 抽卡后端 — unctions/api/draw.js 随机抽取已解锁图片
- [x] PWA manifest — public/manifest.webmanifest 已就绪
- [x] 自定义 404 页面 — NotFoundView.vue（毛玻璃风格）+ router catch-all
- [x] 许愿墙 Mock 补齐 + 反馈 Mock — vite dev 完整链路
- [x] 提示词过滤分组 — 同一串不同变体自动归一（filter_words 表 + grouping.js）
- [x] 反馈管理 — AdminView 反馈列表查看 + 删除
- [x] 批次密码重置 backend — POST /api/admin/batches/:batch_id/reset-password
- [x] 用户管理 — AdminView 用户列表 + 提权/降权（backend + frontend）
- [x] 下载记录详情 — AdminView 下载日志 + 按用户/批次筛选 + 分页
- [x] 批次密码重置 — AdminView 批次设置面板一键重置密码
- [x] 图片懒加载全量覆盖 — ImageGrid/FlipBook/WaterfallGrid/FavoritesView
- [x] ModuleNav 滑块闪现修复 — 初始 opacity:0，ready 后才显示
- [x] Vibe/提示词列表无限滚动分页 — IntersectionObserver（backend + frontend 均已有）
- [x] Vibe/提示词详情页删帖 — VibePostView + PromptPostView 编辑模式删除按钮
- [x] Discord 身份组验证 — OAuth scope 扩展 + callback 公会/角色检查 + LandingView 错误提示
- [x] AdminView 手机端更多菜单 — 方案 B 下拉菜单

---

### 🐛 待修复

（无已知 bug — 所有已知问题已修复 ✅）

### 🖆 待做新功能

（所有新功能已完成 ✅）

### ⚡ 待优化

（所有优化已完成 ✅）

---

## 五、数据组织方式

```
画廊（The Glasshouse）
  └─ 批次（Batch）— 如 "2026-10 月度合集"，可自定义封面和备注
       └─ Prompt 组（prompt_groups）— 上传时自动按正负面 prompt 聚类，可手动调整
            └─ 图片（Image）— 含 metadata（prompt、seed、采样器、CFG 等）
```

- 批次按时间组织，由管理员在创建时命名
- 浏览端内页展示分组列表（组名 + 备注），网格/翻页书按分组展示图片
- 分组信息在后端上传流程中自动生成、可手动调整

---

## 六、设计语言

- 品牌色：柔和大地色系（米白 #F7F1E5、鼠尾草绿 #C6D0B4、橄榄绿 #7A8B64）
- 暗色模式：深黑底 #0A0A0A + 暖棕灰字
- 玻璃质感：backdrop-filter blur + 半透明 border
- 圆角为主（16px-24px），避免直角
- 动画平滑（cubic-bezier 0.4/0/0.2/1）
- 响应式：PC 左上悬浮导航、移动端底部通栏导航
